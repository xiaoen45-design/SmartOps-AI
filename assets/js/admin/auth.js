/**
 * SmartOps Source: assets/js/admin/auth.js
 * Purpose: Js / Admin: Auth client-side interactions and dynamic UI behaviour.
 * Developer Guide: named functions are separated into searchable sections.
 */

// -----------------------------------------------------------------------------
// SECTION: Login Form Initialization
// -----------------------------------------------------------------------------
document.addEventListener('DOMContentLoaded', () => {
  const firstInput = document.querySelector('input');
  if (firstInput) firstInput.focus();
});
